<div class="container my-5">
 <div class="row">
   <h3>マイページ</h3>
   <h3>会員情報          <%= link_to "編集する", edit_public_user_path(@user.id), class: "btn btn-success" %></h3>
   <table class= "table">
    <tr>
      <th>氏名</th>
      <td><%= @user.last_name %><%= @user.first_name %></td>
    </tr>
    <tr>
　　 <th>フリガナ</th>
     <td><%= @user.last_name_kana %><%= @user.first_name_kana %></td>
    </tr>
    <tr>
     <th>メールアドレス</th>
     <td><%= @user.email %></td>
    </tr>
   </table>
   <%= link_to "お気に入り一覧", favorites_public_user_path(@user)%>
 </div>
</div>